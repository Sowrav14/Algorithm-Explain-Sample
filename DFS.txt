vector<int>g[N];

void dfs(int u, int p = -1){
    // After entering a vertex...
    for(auto v : g[u]){
        // before entering the child...
        if(v != p) dfs(v, u);
        // after exitting the child...
    }
    // Before leaving the vertex...
}